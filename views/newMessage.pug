extends layouts/main-layout.pug

block content
  .chatPageContainer
    .chatTitleBar
      label(for="userSearchTextbox") Para:
      #selectedUsers
        input#userSearchTextbox(type="text", placeholder="Digite o nome da pessoa")
    .resultsContainer
    button#createChatButton(disabled) Criar conversação